// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyAUIDl2rDp4qaPJOP0Q2rCM-y88QyMjypM",
    authDomain: "ng-electron.firebaseapp.com",
    databaseURL: "https://ng-electron.firebaseio.com",
    projectId: "ng-electron",
    storageBucket: "ng-electron.appspot.com",
    messagingSenderId: "481209172410",
    appId: "1:481209172410:web:1f8c4086d3985fa1"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
